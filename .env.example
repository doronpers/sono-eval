# Sono-Eval Configuration
#
# ⚠️ SECURITY WARNING: This is an EXAMPLE configuration file!
#
# IMPORTANT:
#   1. Copy this file to .env: cp .env.example .env
#   2. NEVER commit .env to version control (it's in .gitignore)
#   3. CHANGE all default/example values before production use
#   4. Generate strong random keys for SECRET_KEY and SUPERSET_SECRET_KEY
#   5. Use strong passwords for database and Redis
#
# 🔒 To generate secure keys:
#   SECRET_KEY:          python -c "import secrets; print(secrets.token_urlsafe(32))"
#   SUPERSET_SECRET_KEY: openssl rand -base64 42
#
# See SECURITY.md for detailed security guidelines.

# Application
APP_NAME=sono-eval
APP_ENV=development
DEBUG=true
LOG_LEVEL=INFO

# API Server
API_HOST=0.0.0.0
API_PORT=8000
API_WORKERS=4

# Database
DATABASE_URL=sqlite:///./sono_eval.db
# For PostgreSQL: postgresql://user:STRONG_PASSWORD@localhost:5432/sono_eval
# ⚠️ NEVER use default passwords like "postgres" or "password" in production!

# Redis (for caching and task queues)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=
# ⚠️ For production, set a strong REDIS_PASSWORD

# MemU Configuration
MEMU_STORAGE_PATH=./data/memory
MEMU_MAX_DEPTH=5
MEMU_CACHE_SIZE=1000

# T5 Model Configuration
T5_MODEL_NAME=t5-base
T5_CACHE_DIR=./models/cache
T5_LORA_RANK=8
T5_LORA_ALPHA=16
T5_LORA_DROPOUT=0.1

# TagStudio Configuration
TAGSTUDIO_ROOT=./data/tagstudio
TAGSTUDIO_AUTO_TAG=true

# Assessment Configuration
ASSESSMENT_ENGINE_VERSION=1.0
ASSESSMENT_ENABLE_EXPLANATIONS=true
ASSESSMENT_MULTI_PATH_TRACKING=true
DARK_HORSE_MODE=enabled

# Superset Configuration
SUPERSET_HOST=localhost
SUPERSET_PORT=8088
SUPERSET_SECRET_KEY=change_this_secret_key_in_production
# ⚠️ MUST generate a strong random key for production! Use: openssl rand -base64 42

# Security
SECRET_KEY=your-secret-key-here-change-in-production
# ⚠️ MUST generate a strong random key for production! Use: python -c "import secrets; print(secrets.token_urlsafe(32))"
ALLOWED_HOSTS=localhost,127.0.0.1

# File Upload
MAX_UPLOAD_SIZE=10485760
ALLOWED_EXTENSIONS=py,js,ts,java,cpp,c,go,rs,rb

# Batch Processing
BATCH_SIZE=32
MAX_CONCURRENT_ASSESSMENTS=4
